# v8.9

## ⏰ یادآور ساعت پیک (گران) پروایدرها
- اگر لحظه‌ای که پرامپت می‌فرستی در بازهٔ **پیک مصرف** پروایدر فعال باشی، ربات قبل از اجرا هشدار می‌دهد و می‌پرسد «ادامه بدهم؟» تا خودت تصمیم بگیری.
- اطلاعات پیک **DeepSeek** به‌صورت داخلی ثبت شده (به وقت ایران) و برای بقیهٔ پروایدرها هنگام افزودن کلید API پیشنهاد شناسایی/فعال‌سازی می‌شود.
- در **پایان هر چت** وضعیت پیک پروایدر همان مکالمه نمایش داده می‌شود: 🟢 خارج از پیک یا 🔴 همین حالا در پیک (با ساعت پایان).
- پشتیبانی از بازه‌های عبورکننده از نیمه‌شب و روزهای هفته.

## 🔄 سینک زندهٔ فهرست مدل‌ها با خود opencode
- کاتالوگ مدل/پروایدر دیگر فقط از models.dev خوانده نمی‌شود؛ مستقیماً از endpoint استاندارد `/provider` خود سرور opencode هم سینک می‌شود.
- یعنی **پروایدرها و مدل‌های سفارشی کانفیگ** تو هم در فهرست ربات دیده می‌شوند و همیشه با واقعیت سرور هم‌گام است.
- در فهرست مدل‌ها حالا **نام خوانا** مدل هم کنار شناسه‌اش نشان داده می‌شود.

## 🗑 امکانات سریع‌تر
- **حذف سریع نشست**: زیر پیام خلاصهٔ پایان چت، دکمهٔ `🗑 حذف این نشست` (فعال تا ۱۰ ثانیه) بدون رفتن به منوی `🗂 نشست‌ها`.
- **حذف کلید API**: در صفحهٔ کلیدها کنار هر کلید فعال دکمهٔ `🗑` اضافه شد؛ بعد از حذف، سرور خودکار ری‌استارت می‌شود.

## نصب / آپدیت
```bash
curl -fsSL https://raw.githubusercontent.com/Aknuun/GAZAN-opencode-bot/master/install.sh -o /tmp/opencode-tg-install.sh && sudo bash /tmp/opencode-tg-install.sh
```

**Full Changelog**: https://github.com/Aknuun/GAZAN-opencode-bot/compare/v8.8...v8.9
